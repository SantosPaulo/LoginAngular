/* import { ValidateLoginDirective } from './validate-login.directive';

describe('ValidateLoginDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateLoginDirective();
    expect(directive).toBeTruthy();
  });
}); */
