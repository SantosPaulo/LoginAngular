/* import { ValidateSigninDirective } from './validate-signin.directive';

describe('ValidateSigninDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateSigninDirective();
    expect(directive).toBeTruthy();
  });
});
 */